import { CalendarIntegration } from "@/components/calendar-integration";

export default function CalendarPage() {
  return (
    <div className="page-gutter min-h-screen bg-background pb-20" style={{ paddingTop: "7rem" }}>
      <CalendarIntegration />
    </div>
  );
}